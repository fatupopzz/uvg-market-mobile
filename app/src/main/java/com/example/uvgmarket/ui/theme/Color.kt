package com.example.uvgmarket.ui.theme

import androidx.compose.ui.graphics.Color

//COLORES PRIMARIOS
val UvgGreenPrimary = Color(0xFF355E34)      // Verde principal
val UvgGreenDark = Color(0xFF264425)         // Verde oscuro para fondos
val UvgGreenMedium = Color(0xFF99B697)       // Verde medio para acentos
val UvgGreenLight = Color(0xFFEAF0E8)        // Verde claro para backgrounds

// SUPERFICIE
val BackgroundWhite = Color(0xFFFEFEFE)      // Fondo blanco principal
val SurfaceWhite = Color(0xFFFFFFFF)         // Superficie blanca pura
val SurfaceDark = Color(0xFF1C1C1C)          // Superficie oscura
val SurfaceOverlay = Color(0x80000000)       // Overlay semitransparente

// TEXTO (OPCIONAL)
val TextPrimary = Color(0xFF355E34)          // Texto principal (verde UVG)
val TextSecondary = Color(0xFF666666)        // Texto secundario
val TextWhite = Color(0xFFFFFFFF)            // Texto blanco
val TextDark = Color(0xFF1C1C1C)             // Texto oscuro
val TextPlaceholder = Color(0xFF999999)      // Placeholder en campos

// === COLORES DE BOTONES ===
val ButtonPrimary = Color(0xFF355E34)        // Botón principal
val ButtonSecondary = Color(0xFFFFFFFF)      // Botón secundario
val ButtonDisabled = Color(0xFFCCCCCC)       // Botón deshabilitado

// === COLORES DE ESTADO ===
val Success = Color(0xFF4CAF50)              // Éxito (verde)
val Error = Color(0xFFFF5252)                // Error (rojo)
val Warning = Color(0xFFC107)                // Advertencia (amarillo)
val Info = Color(0xFF2196F3)                 // Información (azul)

// === COLORES LEGACY (mantener por compatibilidad) ===
val Purple80 = Color(0xFFD0BCFF)
val PurpleGrey80 = Color(0xFFCCC2DC)
val Pink80 = Color(0xFFEFB8C8)

val Purple40 = Color(0xFF6650a4)
val PurpleGrey40 = Color(0xFF625b71)
val Pink40 = Color(0xFF7D5260)